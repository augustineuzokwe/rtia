{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install --with-deps chromium",
    "backends:start": "bash scripts/start-backends.sh",
    "backends:stop": "bash scripts/stop-backends.sh"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "@types/node": "^25.9.3",
    "playwright": "1.60.0",
    "typescript": "^5.6.3"
  }
}
